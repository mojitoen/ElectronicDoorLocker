{
  "name": "authentication-server",
  "version": "1.0.0",
  "description": "A server designed to take requests from an app and a keypad to unlock a component in arduino",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.2"
  }
}
